#include <iostream>
using namespace std;

int main() 
{
  int x[5];

  cout << "Enter 5 integers as input: " << endl;

  for (int i = 0; i < 5; ++i) {
    cin >> x[i];
  }

  cout << "The array elements are: ";

  for (int n = 0; n < 5; ++n) {
    cout << x[n] << "  ";
  }

  return 0;
}
